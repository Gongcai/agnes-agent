{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "agnes-agent 默认权限：核心事件 + 窗口。工具执行/SSH 等权限后续按需在此收紧。",
  "windows": ["main", "quick"],
  "permissions": [
    "core:default",
    "core:event:default",
    "dialog:default",
    "global-shortcut:default"
  ]
}
